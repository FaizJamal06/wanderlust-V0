<nav class="navbar navbar-expand-lg bg-body-light sticky-top">
  <div class="container-fluid">
    <a class="navbar-brand" href="/listings">
      <i class="fas fa-compass fa-rotate-270" style="color: #B197FC;"></i>
      
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-link" href="/listings">Explore</a>
      </div>

      <!-- Centered search form -->
      <div class="mx-auto d-none d-lg-flex" style="width: 480px;">
        <form class="d-flex w-100" role="search" action="/listings" method="GET">
          <input class="form-control me-2 navbar-search-input" name="q" type="search" placeholder="Search listings, locations..." aria-label="Search"/>
          <button class="btn btn-primary navbar-search-btn" type="submit">Search</button>
        </form>
      </div>

      <div class="navbar-nav ms-auto">
        <a class="nav-link" href="/listings/new">New Listing</a>
        <% if (!currentUser) { %>
          <a class="nav-link" href="/signup"><b>Sign up</b></a>
          <a class="nav-link" href="/login"><b>Log in</b></a>
        <% } else { %>
          <form action="/logout" method="post" class="d-inline">
            <button class="nav-link btn btn-link" type="submit"><b>Logout</b></button>
          </form>
        <% } %>
      </div>

      <!-- Mobile search inside collapsed navbar (visible on small screens) -->
      <div class="w-100 d-lg-none mt-2">
        <form class="d-flex" role="search" action="/listings" method="GET">
          <input class="form-control me-2 navbar-search-input" name="q" type="search" placeholder="Search listings, locations..." aria-label="Search"/>
          <button class="btn btn-primary navbar-search-btn-mobile" type="submit" aria-label="Search">
            <i class="fas fa-search"></i>
          </button>
        </form>
      </div>
    </div>
  </div>
</nav>